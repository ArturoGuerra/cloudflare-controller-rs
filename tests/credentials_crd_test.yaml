apiVersion: cloudflare.ar2ro.io/v1
kind: Credentials
metadata:
    name: crd-test-token
spec:
  accountId: "Account ID"
  auth:
    userAuthToken: "api token"
---
apiVersion: cloudflare.ar2ro.io/v1
kind: Credentials
metadata:
    name: crd-test-email
spec:
  accountId: "Account ID"
  auth:
    userAuthKey:
      email: "ar2roguerra@gmail.com"
      key: "key goes here"
---
apiVersion: cloudflare.ar2ro.io/v1
kind: Credentials
metadata:
    name: crd-test-service-key
spec:
  accountId: "Account ID"
  auth:
    serviceKey: "service key"
